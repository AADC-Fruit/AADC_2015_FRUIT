Linux only.
Build tapir and clsquare first (mkdir build && cd build && make install).
Install dependencies as needed.
Consult documentation at ml.informatik.uni-freiburg.de in Software section in case of problems (newer software versions available).
Add symlinks to tapir and clsquare binaries in aadc-demo before using demo scripts or adjust paths.
